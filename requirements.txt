# 模型调用（默认 claude 后端）
# 透过 Claude Code CLI 用现有订阅鉴权，不需要单独的 API key。
# 包体较大（约 85 MB），因为它把 CLI 打包在内。
claude-agent-sdk>=0.2.150

# zhipu 后端用（config.yaml 里 api.provider 改成 zhipu 才需要）
# 直连 REST 而不用官方 SDK：SDK 把 pyjwt 钉在 <2.9.0，
# 与 mcp 要求的 >=2.10.1 冲突，见 llm.py
httpx>=0.27

# 本地文本抽取 —— macOS 原生能力，零成本替代多模态 OCR
# AppKit / Foundation: 取前台应用
# ApplicationServices: AXUIElement 无障碍树
# Quartz: 图像读取
# Vision: VNRecognizeTextRequest 本地 OCR
pyobjc-framework-Cocoa>=12.0
pyobjc-framework-ApplicationServices>=12.0
pyobjc-framework-Quartz>=12.0
pyobjc-framework-Vision>=12.0

# MCP 服务端 —— 供 Claude Code 等客户端查询屏幕记忆
mcp>=2.1.0

# 检索
chromadb>=1.0.0

# 基础设施
pyyaml>=6.0
schedule>=1.2
pillow>=11.0
flask
textual
