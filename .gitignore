# ── 个人数据：绝不入库 ──────────────────────────
# 截图、无障碍文本、OCR 缓存都是真实屏幕内容
screenshots/
# 报告是对屏幕活动的详细描述，同样是个人数据
reports/
# 看板批注
# 脱敏术语表：内含真实同事姓名与公司名
redact.local.yaml
# 向量库
chroma_db/

# ── 凭证 ──────────────────────────────────────
.env

# ── 运行时产物 ────────────────────────────────
capture.pid
*.log
*.log.*
__pycache__/
*.pyc
venv/
.DS_Store

# ── 历史备份（重建 git 历史时保留的旧仓库）────
.git.bak-*
.paused-until
deletions.log
index.db
index.db-wal
index.db-shm
